interface:
  display_name: "Authio Cli"
  short_description: "Repo-specific patterns and workflows for authio_cli"
  default_prompt: "Use the authio_cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true